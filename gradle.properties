# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
# 本机 JAVA_HOME 也指向 zulu-25；此行为其他机器编译兜底。
# 注意：该路径为机器相关，若在其他环境构建请删除此行或改成对应 JDK25 路径。
org.gradle.java.home=C:\\Program Files\\Zulu\\zulu-25
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=26.1.2
loader_version=0.19.3
loom_version=1.17.19

# Mod Properties
mod_version=1.0.4
maven_group=anima
archives_base_name=anima

# Dependencies
fabric_api_version=0.155.2+26.1.2

# Mod Menu (compile only, optional at runtime; removed from production jar)
modmenu_version=18.0.0
